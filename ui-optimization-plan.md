# 融智云考助手：功能与 UI 优化方案

## 审核结论

当前产品的核心链路是“打开云考网页 → 自动填充 → 循环提取 → AI 补全 → 导出”。业务闭环已经具备，问题主要集中在工作流信息被压缩到 320px 悬浮窗里：

1. 用户看不到稳定的总量、增量、待复核量和导出结果，提取过程中只能依赖一段不断变化的状态文字。
2. “开始提取 / 清空 / 导出 / 练习版”处于同一层级，缺少主次关系；导出依赖状态判断，但用户不容易知道为什么按钮不可用。
3. 过程状态没有阶段模型。OCR、题目解析、结构化、AI 校验本质上是四个阶段，却被合并成“正在提取”。
4. 设置页已有账号、导出、AI 三个分组，但缺少“当前配置是否完整”的反馈，也没有把高频导出选项放到工作台上。
5. 代码层面 `process_html_with_bs4()` 在 GUI 线程内解析完整 DOM，题目较多时可能造成界面卡顿；AI 补全按题目直接创建线程，缺少并发上限。
6. 清空题库时直接重置 `pending_ai_workers` 容器，正在运行的 QThread 仍可能回调，存在生命周期管理风险；导出线程也直接持有题目列表，导出期间数据发生变化会造成结果不稳定。

## 优先级建议

### P0：先修稳定性和“看得懂”

- 给提取状态建立明确的 `idle / running / paused / completed / error` 状态机，按钮、状态栏、进度文本统一从状态机派生。
- 导出前对题目做深拷贝快照；清空题库时只让旧 AI 任务失效，保留线程对象直到 `finished`，避免运行中的线程被提前释放。
- 将 HTML 解析移到 `QThread` 或 `QThreadPool`，主线程只负责接收解析结果和更新 UI。
- 把“已提取、本次新增、AI 待复核、导出文件”四项数据放到工作台首屏。

### P1：减少点击和等待焦虑

- 右侧只保留三个快捷动作：开始/暂停提取、练习版、导出题库；“清空”移入更多操作并加确认，避免误触。
- 加入阶段进度：OCR 识别 → 题目解析 → 结构化 → AI 校验，并显示当前文件、已用时、预计剩余。
- 加入 `Ctrl/Cmd + K` 命令面板，支持开始/暂停、导出、练习版和帮助，熟练用户无需频繁拖动悬浮窗。
- 导出弹窗先选格式，再显示“答案与解析 / 练习版 / 自动打开”的最终摘要，减少导出后才发现选项错误。

### P2：提高批量处理效率

- AI 补全改为有上限的任务队列，默认并发 3 个；对 429、超时和网络错误做指数退避，并在 UI 显示“待处理 / 重试中 / 需复核”。
- 解析侧尽量从网页 DOM 只提取题目容器，而不是每次复制完整 `document.body.innerHTML`；图片 URL 做缓存，避免重复下载。
- 导出记录落地到本地轻量 JSON/SQLite，支持重试、打开文件夹、复制路径和按状态筛选。
- 对重复题使用“题目 ID 优先、标题+选项签名兜底”的去重策略，并在界面显示“跳过重复”数量。

## UI 设计原则

- 视觉：真白背景 + 浅雾蓝表面 + 薄荷绿成功状态；玻璃效果只使用低透明度、轻边框和轻微模糊，不使用深色宇宙背景、霓虹光晕或大面积渐变。
- 信息：首屏只放会影响下一步决策的信息，不堆装饰性指标；主按钮始终只有一个。
- 层级：工作台负责“当前任务”和“最近结果”，设置负责低频配置，命令面板负责快捷入口。
- 反馈：所有耗时操作都有阶段、进度、剩余时间和可恢复状态；完成、暂停、失败都给出明确下一步。
- 响应式：宽屏采用左导航 + 主内容 + 右快捷操作；窄屏将右侧操作移到主内容下方，表格保持横向滚动，不强行压缩列。

## 优秀案例提炼

- Raycast 的核心经验是把高频动作集中到可搜索的命令入口，并在底部动作栏里统一操作反馈；适合转化为本项目的 `Ctrl/Cmd + K` 命令面板。
- Linear 将命令按当前上下文分组，并允许用键盘、命令菜单和右键完成相同动作；本项目可以让“当前任务、导出、练习版”在不同状态下动态排序。
- Notion 采用克制的侧边栏和可召回的命令搜索，减少用户在多窗口之间切换；本项目保留云考网页作为工作区，但把题库状态收敛到自己的轻量工作台。

## 交付物

- `ui/dashboard_preview.html`：可离线直接打开的完整 UI 预览，包含快捷命令、提取状态、练习版切换、导出格式弹窗、导航、Toast 反馈和响应式布局。
- 视觉参考：采用小清新工作台方向，后续可将 HTML 的 token 映射到 PySide6 QSS；Qt 原生 QSS 本身不支持 `backdrop-filter`，桌面端建议使用半透明表面 + 轻阴影模拟，真正的模糊效果保留在 HTML 预览或 QWebEngine 页面中。

