<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="YunKao.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:YunKao.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="融智云考"
    mc:Ignorable="d">
  <Grid x:Name="RootSurface" Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border x:Name="AppTitleBar"
            Grid.Row="0"
            Background="{StaticResource TitleBarBrush}"
            Padding="16,0,144,0">
      <Grid VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Spacing="9">
          <Border Width="26" Height="26" CornerRadius="9" Background="{StaticResource BluePrimaryBrush}">
            <TextBlock Text="Y" FontSize="14" FontWeight="SemiBold" Foreground="White"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <StackPanel VerticalAlignment="Center" Spacing="-2">
            <TextBlock Text="融智云考" Style="{StaticResource TitleTextStyle}" />
            <TextBlock Text="Desktop Workspace" Style="{StaticResource CaptionTextStyle}" />
          </StackPanel>
        </StackPanel>

        <Border HorizontalAlignment="Right" Background="#42CDEFF2" BorderBrush="#5CFFFFFF" BorderThickness="1" CornerRadius="16" Padding="9,4">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <Ellipse x:Name="TaskStatusIndicator" Width="6" Height="6" Fill="{StaticResource CyanPrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock x:Name="TaskStatusText" Text="本地就绪" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Padding="10,2,10,12" ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="NavigationColumn" Width="68" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Grid x:Name="NavigationSurface" Grid.Column="0">
        <SystemBackdropElement x:Name="NavigationBackdrop" CornerRadius="20">
          <SystemBackdropElement.SystemBackdrop>
          <DesktopAcrylicBackdrop />
          </SystemBackdropElement.SystemBackdrop>
        </SystemBackdropElement>
        <Border Background="{StaticResource RailSurfaceBrush}"
                BorderBrush="{StaticResource LiquidInnerBorderBrush}"
                BorderThickness="1"
                CornerRadius="20" />
        <controls:NavigationRail x:Name="NavigationRail" />
      </Grid>

      <Frame x:Name="RootFrame"
             Grid.Column="1"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             Background="Transparent" />
    </Grid>
  </Grid>
</Window>
